<!DOCTYPE html>

<html lang="en" dir="ltr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>فوائد · Benefits</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Amiri:ital,wght@0,400;0,700;1,400&family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Noto+Naskh+Arabic:wght@400;500;700&family=Noto+Sans:wght@400;500&family=Noto+Sans+Bengali:wght@400;500&family=Noto+Sans+Devanagari:wght@400;500&family=Noto+Nastaliq+Urdu:wght@400;700&display=swap" rel="stylesheet">
<style>
:root {
--ink: #1a1209;
--parchment: #f5eed8;
--cream: #faf6ec;
--gold: #b8860b;
--gold-lt: #d4a843;
--gold-pale: #f0e0a0;
--green: #1b3a2e;
--green-mid: #2d5c47;
--green-lt: #4a8c6f;
--rust: #8b3a1a;
--border: rgba(184,134,11,0.25);
--shadow: rgba(26,18,9,0.15);
}
*, *::before, *::after { box-sizing:border-box; margin:0; padding:0; }
html { scroll-behavior:smooth; }
body {
font-family:'Cormorant Garamond',Georgia,serif;
background:var(--cream); color:var(--ink);
min-height:100vh; overflow-x:hidden;
}
body::before {
content:''; position:fixed; inset:0;
background-image:
repeating-linear-gradient(45deg, transparent,transparent 40px,rgba(184,134,11,.04) 40px,rgba(184,134,11,.04) 41px),
repeating-linear-gradient(-45deg,transparent,transparent 40px,rgba(184,134,11,.04) 40px,rgba(184,134,11,.04) 41px);
pointer-events:none; z-index:0;
}
.ornament { display:block; margin:0 auto; fill:var(--gold); opacity:.7; }

/* ── PAGES ── */
.page { display:none; position:relative; z-index:1; }
.page.active { display:block; }

/* ══ HOME ══ */
#home {
min-height:100vh; display:flex; flex-direction:column;
align-items:center; justify-content:center;
padding:3rem 1.5rem; text-align:center;
}
.site-title { font-family:‘Amiri’,serif; font-size:clamp(3.5rem,10vw,6rem); font-weight:700; color:var(–green); line-height:1; margin-top:1.5rem; }
.site-title-ar { font-family:‘Noto Naskh Arabic’,serif; font-size:clamp(2.5rem,7vw,4.5rem); color:var(–gold); display:block; margin-top:.3rem; }
.site-sub { font-style:italic; font-size:1.25rem; color:var(–green-mid); margin-top:.8rem; letter-spacing:.12em; }
.divider-line { width:160px; height:1px; background:linear-gradient(90deg,transparent,var(–gold),transparent); margin:2.5rem auto; }

.cards { display:grid; grid-template-columns:repeat(auto-fit,minmax(210px,1fr)); gap:2rem; max-width:900px; width:100%; margin-top:1rem; }
.card {
background:var(–parchment); border:1px solid var(–border); border-radius:4px;
padding:2.5rem 2rem; cursor:pointer;
transition:transform .3s,box-shadow .3s,background .3s;
box-shadow:0 2px 12px var(–shadow); position:relative; overflow:hidden;
}
.card::before {
content:’’; position:absolute; top:0;left:0;right:0; height:3px;
background:linear-gradient(90deg,var(–gold),var(–green-lt),var(–gold));
transform:scaleX(0); transition:transform .3s;
}
.card:hover::before { transform:scaleX(1); }
.card:hover { transform:translateY(-6px); box-shadow:0 12px 32px var(–shadow); background:#fff; }
.card-icon { font-size:2.8rem; margin-bottom:1rem; display:block; }
.card-title-en { font-size:1.6rem; font-weight:600; color:var(–green); letter-spacing:.03em; }
.card-title-ar { font-family:‘Noto Naskh Arabic’,serif; font-size:1.3rem; color:var(–gold); display:block; margin-top:.3rem; }
.card-desc { font-size:.95rem; color:#5a4a30; margin-top:.8rem; font-style:italic; line-height:1.6; }
.card-count { display:inline-block; margin-top:1.2rem; font-size:.78rem; letter-spacing:.12em; text-transform:uppercase; color:var(–green-mid); border:1px solid var(–border); padding:.2rem .7rem; border-radius:2px; }

/* ══ INNER PAGES ══ */
.inner-page { max-width:940px; margin:0 auto; padding:2rem 1.5rem 5rem; }
.page-header { padding:3rem 0 2rem; text-align:center; position:relative; }
.back-btn {
position:absolute; top:3rem; left:0;
background:none; border:1px solid var(–border); color:var(–green);
font-family:‘Cormorant Garamond’,serif; font-size:1rem;
padding:.4rem 1rem; cursor:pointer; border-radius:2px; transition:all .2s; letter-spacing:.05em;
}
.back-btn:hover { background:var(–green); color:var(–parchment); }
.page-title-en { font-family:‘Amiri’,serif; font-size:clamp(2.2rem,6vw,3.5rem); color:var(–green); line-height:1; }
.page-title-ar { font-family:‘Noto Naskh Arabic’,serif; font-size:clamp(1.6rem,4vw,2.5rem); color:var(–gold); }

.add-btn {
display:flex; align-items:center; gap:.5rem;
background:var(–green); color:var(–gold-pale); border:none;
font-family:‘Cormorant Garamond’,serif; font-size:1.05rem; letter-spacing:.05em;
padding:.7rem 1.6rem; border-radius:3px; cursor:pointer;
margin:0 auto 2rem; transition:background .2s,transform .2s;
}
.add-btn:hover { background:var(–green-mid); transform:translateY(-2px); }

.search-wrap { position:relative; margin-bottom:2rem; }
.search-wrap input {
width:100%; background:var(–parchment); border:1px solid var(–border);
border-radius:3px; padding:.65rem 1rem .65rem 2.8rem;
font-family:‘Cormorant Garamond’,serif; font-size:1.05rem; color:var(–ink);
}
.search-wrap input:focus { outline:none; border-color:var(–gold); }
.search-icon { position:absolute; left:.9rem; top:50%; transform:translateY(-50%); color:var(–gold); pointer-events:none; }

/* ── BENEFIT CARD ── */
.benefits-list { display:flex; flex-direction:column; gap:1.8rem; }
.benefit-card {
background:var(–parchment); border:1px solid var(–border); border-radius:4px;
box-shadow:0 2px 10px var(–shadow); position:relative;
animation:fadeUp .4s ease both;
}
@keyframes fadeUp { from{opacity:0;transform:translateY(12px)} to{opacity:1;transform:none} }

.card-main { padding:1.8rem 2.2rem 1.4rem; }
.tag { display:inline-block; font-size:.7rem; letter-spacing:.12em; text-transform:uppercase; padding:.2rem .6rem; border-radius:2px; margin-bottom:.8rem; }
.tag-verdict { background:var(–green); color:var(–gold-pale); }
.tag-audio { background:var(–rust); color:#f5eed8; }
.tag-text { background:var(–gold); color:var(–ink); }
.benefit-card h4 { font-family:‘Amiri’,serif; font-size:1.35rem; color:var(–green); margin-bottom:.4rem; }
.benefit-card .scholar { font-style:italic; font-size:.9rem; color:var(–gold); margin-bottom:.7rem; }
.benefit-card .body-text { font-size:1rem; line-height:1.8; color:#3a2e1a; }
.benefit-card .arabic-text { font-family:‘Noto Naskh Arabic’,serif; font-size:1.25rem; line-height:2; text-align:right; color:var(–ink); margin-top:.8rem; border-top:1px solid var(–border); padding-top:.8rem; direction:rtl; }
.benefit-card .en-translation { font-style:italic; color:#5a4a30; margin-top:.5rem; font-size:.95rem; line-height:1.7; }
.embed-area { margin-top:1rem; border-radius:4px; overflow:hidden; background:var(–green); padding:1rem; display:flex; flex-direction:column; gap:.6rem; }
.embed-area audio, .embed-area video { width:100%; border-radius:3px; }
.embed-url { font-size:.82rem; color:var(–gold-pale); word-break:break-all; opacity:.8; }
.card-date { margin-top:.9rem; font-size:.75rem; color:#8a7050; letter-spacing:.06em; }
.delete-btn { position:absolute; top:1rem; right:1rem; background:none; border:none; color:#c0a0a0; font-size:1.1rem; cursor:pointer; transition:color .2s; z-index:2; }
.delete-btn:hover { color:var(–rust); }

/* ══ TRANSLATIONS PANEL ══ */
.translations-panel { border-top:2px solid var(–border); }

.trans-panel-header {
display:flex; align-items:center; gap:.8rem;
padding:.6rem 1.2rem .5rem;
background:rgba(27,58,46,.05);
border-bottom:1px solid var(–border);
}
.trans-panel-label {
font-size:.7rem; letter-spacing:.14em; text-transform:uppercase;
color:var(–green-mid); font-weight:600; flex-shrink:0;
}

/* language tab strip */
.lang-tabs {
display:flex; flex-wrap:wrap; gap:.3rem; align-items:center; flex:1;
}
.lang-tab {
display:flex; align-items:center; gap:.3rem;
font-size:.78rem; padding:.25rem .7rem; cursor:pointer;
border-radius:20px; border:1px solid var(–border);
color:#5a4a30; transition:all .15s;
font-family:‘Cormorant Garamond’,serif; white-space:nowrap;
}
.lang-tab:hover { border-color:var(–green-mid); color:var(–green); background:rgba(255,255,255,.6); }
.lang-tab.active { background:var(–green); color:var(–gold-pale); border-color:var(–green); }
.vetted-dot { width:5px; height:5px; border-radius:50%; background:#5fc47a; flex-shrink:0; }
.draft-dot { width:5px; height:5px; border-radius:50%; background:var(–gold-lt); flex-shrink:0; }
.lang-tab.active .vetted-dot { background:#a0ffa0; }
.lang-tab.active .draft-dot { background:var(–gold-pale); }

.add-trans-btn {
margin-left:auto; font-size:.73rem; color:var(–green-mid);
background:none; border:1px dashed rgba(27,58,46,.4); border-radius:20px;
padding:.22rem .7rem; cursor:pointer;
font-family:‘Cormorant Garamond’,serif; letter-spacing:.04em;
transition:all .15s; white-space:nowrap; flex-shrink:0;
}
.add-trans-btn:hover { background:var(–green); color:var(–gold-pale); border-color:var(–green); }

/* content pane */
.lang-content { display:none; padding:1.2rem 2rem 1.5rem; }
.lang-content.active { display:block; }

.trans-meta {
display:flex; align-items:center; gap:.5rem; margin-bottom:.6rem; flex-wrap:wrap;
}
.trans-lang-name { font-size:.75rem; letter-spacing:.1em; text-transform:uppercase; color:var(–green-mid); }
.vetted-badge { font-size:.65rem; background:var(–green); color:var(–gold-pale); padding:.15rem .5rem; border-radius:2px; letter-spacing:.08em; }
.draft-badge { font-size:.65rem; background:var(–gold); color:var(–ink); padding:.15rem .5rem; border-radius:2px; letter-spacing:.08em; }

.trans-text { font-size:1.05rem; line-height:1.9; color:#2a2010; }
.trans-text.rtl-text { direction:rtl; text-align:right; font-size:1.2rem; line-height:2.1; }
.trans-notes { font-size:.85rem; font-style:italic; color:#7a6040; margin-top:.6rem; border-top:1px solid var(–border); padding-top:.5rem; }

.trans-actions { margin-top:.8rem; display:flex; gap:.5rem; flex-wrap:wrap; }
.trans-action-btn {
font-size:.75rem; letter-spacing:.06em; padding:.22rem .65rem;
border-radius:2px; cursor:pointer;
font-family:‘Cormorant Garamond’,serif; transition:all .15s;
}
.btn-vett { background:none; border:1px solid var(–green); color:var(–green); }
.btn-vett:hover { background:var(–green); color:var(–gold-pale); }
.btn-edit-t { background:none; border:1px solid var(–gold); color:var(–gold-lt); }
.btn-edit-t:hover { background:var(–gold); color:var(–ink); }
.btn-del-t { background:none; border:1px solid #c08080; color:#a06060; margin-left:auto; }
.btn-del-t:hover { background:var(–rust); border-color:var(–rust); color:#fff; }

.no-trans-pane { padding:1rem 2rem 1.3rem; font-style:italic; color:#8a7050; font-size:.9rem; }

/* ══ MODAL ══ */
.modal-overlay {
display:none; position:fixed; inset:0;
background:rgba(26,18,9,.65); z-index:100;
align-items:center; justify-content:center; padding:1rem;
}
.modal-overlay.open { display:flex; }
.modal {
background:var(–cream); border:1px solid var(–border); border-radius:4px;
padding:2.5rem; width:100%; max-width:580px; max-height:90vh; overflow-y:auto;
box-shadow:0 24px 64px rgba(26,18,9,.35); position:relative;
animation:modalIn .3s ease;
}
@keyframes modalIn { from{opacity:0;transform:translateY(20px)} to{opacity:1;transform:none} }
.modal-close { position:absolute; top:1rem; right:1rem; background:none; border:none; font-size:1.5rem; color:var(–ink); cursor:pointer; }
.modal h3 { font-family:‘Amiri’,serif; font-size:1.6rem; color:var(–green); margin-bottom:1.4rem; }

.field { margin-bottom:1.1rem; }
.field label { display:block; font-size:.8rem; letter-spacing:.1em; text-transform:uppercase; color:var(–green-mid); margin-bottom:.35rem; }
.field input,.field textarea,.field select {
width:100%; background:var(–parchment); border:1px solid var(–border);
border-radius:3px; padding:.6rem .9rem;
font-family:‘Cormorant Garamond’,serif; font-size:1rem; color:var(–ink);
transition:border-color .2s;
}
.field input:focus,.field textarea:focus,.field select:focus { outline:none; border-color:var(–gold); }
.field textarea { min-height:80px; resize:vertical; }
.rtl-toggle { font-size:.72rem; color:var(–gold); cursor:pointer; text-decoration:underline; margin-left:.5rem; }

.vet-row { display:flex; align-items:center; gap:.6rem; margin-bottom:1.2rem; }
.vet-row input[type=checkbox] { accent-color:var(–green); width:16px; height:16px; cursor:pointer; }
.vet-row label { font-size:1rem; cursor:pointer; color:var(–ink); }

.save-btn {
width:100%; background:var(–green); color:var(–gold-pale); border:none;
font-family:‘Cormorant Garamond’,serif; font-size:1.1rem; letter-spacing:.08em;
padding:.8rem; border-radius:3px; cursor:pointer; margin-top:.5rem; transition:background .2s;
}
.save-btn:hover { background:var(–green-mid); }

.empty-state { text-align:center; padding:4rem 2rem; color:#8a7050; font-style:italic; font-size:1.1rem; }
.empty-ornament { font-size:2.5rem; opacity:.35; margin-bottom:1rem; }

.site-footer { text-align:center; padding:1.5rem; font-size:.8rem; color:#8a7050; letter-spacing:.08em; border-top:1px solid var(–border); position:relative; z-index:1; }

::-webkit-scrollbar { width:6px; }
::-webkit-scrollbar-track { background:var(–cream); }
::-webkit-scrollbar-thumb { background:var(–gold); border-radius:3px; }

@media(max-width:600px) {
.back-btn { position:static; margin-bottom:1rem; }
.page-header { padding-top:2rem; }
.modal { padding:1.5rem; }
.card-main,.lang-content { padding-left:1.1rem; padding-right:1.1rem; }
.trans-panel-header { padding:.5rem .8rem; }
}
</style>

</head>
<body>

<!-- ══ HOME ══ -->

<div id="home" class="page active">
<svg class="ornament" width="120" height="48" viewBox="0 0 120 48">
<path d="M60 4 C44 4 28 14 20 28 C12 42 4 44 4 44 C4 44 16 36 20 24 C24 12 40 6 60 6 C80 6 96 12 100 24 C104 36 116 44 116 44 C116 44 108 42 100 28 C92 14 76 4 60 4 Z" opacity=".8"/>
<circle cx="60" cy="28" r="6" fill="none" stroke-width="1.5" stroke="currentColor"/>
<circle cx="60" cy="28" r="3"/>
<line x1="20" y1="44" x2="100" y2="44" stroke-width="1" opacity=".5"/>
<line x1="30" y1="46" x2="90" y2="46" stroke-width=".5" opacity=".3"/>
</svg>
<h1 class="site-title">Benefits<span class="site-title-ar">فوائد</span></h1>
<p class="site-sub">A treasury of Islamic knowledge &amp; verdicts</p>
<div class="divider-line"></div>
<div class="cards">
<div class="card" onclick="navigate('verdicts')">
<span class="card-icon">⚖️</span>
<div class="card-title-en">Verdicts</div>
<span class="card-title-ar">فتاوى</span>
<p class="card-desc">Fatāwā and rulings from major scholars, translated by volunteers.</p>
<span class="card-count" id="verdict-count">0 entries</span>
</div>
<div class="card" onclick="navigate('audio')">
<span class="card-icon">🎙️</span>
<div class="card-title-en">Audio Clips</div>
<span class="card-title-ar">مقاطع صوتية</span>
<p class="card-desc">Audio benefits with translations vetted by language volunteers.</p>
<span class="card-count" id="audio-count">0 entries</span>
</div>
<div class="card" onclick="navigate('text')">
<span class="card-icon">📜</span>
<div class="card-title-en">Text Benefits</div>
<span class="card-title-ar">فوائد نصية</span>
<p class="card-desc">Written benefits and reminders distributed across all languages.</p>
<span class="card-count" id="text-count">0 entries</span>
</div>
</div>
<div class="divider-line"></div>
<svg class="ornament" width="80" height="24" viewBox="0 0 80 24" style="opacity:.4">
<path d="M40 2 L44 10 L52 10 L46 16 L48 24 L40 19 L32 24 L34 16 L28 10 L36 10 Z"/>
</svg>
</div>

<!-- ══ VERDICTS ══ -->

<div id="verdicts" class="page">
<div class="inner-page">
<div class="page-header">
<button class="back-btn" onclick="navigate('home')">← Home</button>
<h2 class="page-title-en">Verdicts</h2>
<div class="page-title-ar">فتاوى</div>
<div class="divider-line" style="margin:1rem auto 0"></div>
</div>
<div class="search-wrap">
<span class="search-icon">🔍</span>
<input type="text" id="verdicts-search" placeholder="Search verdicts…" oninput="renderList('verdicts')">
</div>
<button class="add-btn" onclick="openModal('verdicts')">+ Add Verdict</button>
<div id="verdicts-list" class="benefits-list"></div>
</div>
</div>

<!-- ══ AUDIO ══ -->

<div id="audio" class="page">
<div class="inner-page">
<div class="page-header">
<button class="back-btn" onclick="navigate('home')">← Home</button>
<h2 class="page-title-en">Audio Clips</h2>
<div class="page-title-ar">مقاطع صوتية</div>
<div class="divider-line" style="margin:1rem auto 0"></div>
</div>
<div class="search-wrap">
<span class="search-icon">🔍</span>
<input type="text" id="audio-search" placeholder="Search audio clips…" oninput="renderList('audio')">
</div>
<button class="add-btn" onclick="openModal('audio')">+ Add Audio Clip</button>
<div id="audio-list" class="benefits-list"></div>
</div>
</div>

<!-- ══ TEXT ══ -->

<div id="text" class="page">
<div class="inner-page">
<div class="page-header">
<button class="back-btn" onclick="navigate('home')">← Home</button>
<h2 class="page-title-en">Text Benefits</h2>
<div class="page-title-ar">فوائد نصية</div>
<div class="divider-line" style="margin:1rem auto 0"></div>
</div>
<div class="search-wrap">
<span class="search-icon">🔍</span>
<input type="text" id="text-search" placeholder="Search text benefits…" oninput="renderList('text')">
</div>
<button class="add-btn" onclick="openModal('text')">+ Add Text Benefit</button>
<div id="text-list" class="benefits-list"></div>
</div>
</div>

<!-- ══ MODAL ══ -->

<div class="modal-overlay" id="modal-overlay" onclick="closeModalOutside(event)">
<div class="modal">
<button class="modal-close" onclick="closeModal()">✕</button>
<h3 id="modal-title">Add Benefit</h3>
<div id="modal-body"></div>
</div>
</div>

<footer class="site-footer">
بسم الله الرحمن الرحيم &nbsp;·&nbsp; فوائد · Benefits &nbsp;·&nbsp; All praise is due to Allah
</footer>

<script>
/* ══════════════════════════════════════
LANGUAGE REGISTRY
══════════════════════════════════════ */
const LANGUAGES = [
{ code:'sw', name:'Swahili', native:'Kiswahili', rtl:false, font:'Noto Sans,sans-serif' },
{ code:'ur', name:'Urdu', native:'اردو', rtl:true, font:'Noto Nastaliq Urdu,serif' },
{ code:'ti', name:'Tigrinya', native:'ትግርኛ', rtl:false, font:'Noto Sans,sans-serif' },
{ code:'ru', name:'Russian', native:'Русский', rtl:false, font:'Noto Sans,sans-serif' },
{ code:'ms', name:'Malay', native:'Bahasa Melayu', rtl:false, font:'Noto Sans,sans-serif' },
{ code:'bn', name:'Bengali', native:'বাংলা', rtl:false, font:'Noto Sans Bengali,sans-serif' },
{ code:'hi', name:'Hindi', native:'हिन्दी', rtl:false, font:'Noto Sans Devanagari,sans-serif' },
{ code:'id', name:'Indonesian', native:'Bahasa Indonesia', rtl:false, font:'Noto Sans,sans-serif' },
{ code:'fr', name:'French', native:'Français', rtl:false, font:'Cormorant Garamond,serif' },
{ code:'tr', name:'Turkish', native:'Türkçe', rtl:false, font:'Noto Sans,sans-serif' },
{ code:'so', name:'Somali', native:'Soomaali', rtl:false, font:'Noto Sans,sans-serif' },
{ code:'ha', name:'Hausa', native:'Hausa', rtl:false, font:'Noto Sans,sans-serif' },
{ code:'am', name:'Amharic', native:'አማርኛ', rtl:false, font:'Noto Sans,sans-serif' },
{ code:'ps', name:'Pashto', native:'پښتو', rtl:true, font:'Noto Nastaliq Urdu,serif' },
{ code:'fa', name:'Persian', native:'فارسی', rtl:true, font:'Noto Naskh Arabic,serif' },
{ code:'de', name:'German', native:'Deutsch', rtl:false, font:'Cormorant Garamond,serif' },
{ code:'es', name:'Spanish', native:'Español', rtl:false, font:'Cormorant Garamond,serif' },
{ code:'zh', name:'Chinese', native:'中文', rtl:false, font:'Noto Sans,sans-serif' },
{ code:'pt', name:'Portuguese', native:'Português', rtl:false, font:'Cormorant Garamond,serif' },
{ code:'ta', name:'Tamil', native:'தமிழ்', rtl:false, font:'Noto Sans,sans-serif' },
];
const langMap = {};
LANGUAGES.forEach(l => langMap[l.code] = l);
function langOf(code) { return langMap[code] || { code, name:code, native:code, rtl:false, font:'Noto Sans,sans-serif' }; }

/* ══════════════════════════════════════
STORE
══════════════════════════════════════ */
const KEY = 'islamic_benefits_v2';
function load() {
try { return JSON.parse(localStorage.getItem(KEY)) || { verdicts:[], audio:[], text:[] }; }
catch(e) { return { verdicts:[], audio:[], text:[] }; }
}
function save(d) { localStorage.setItem(KEY, JSON.stringify(d)); }

/* ══════════════════════════════════════
ROUTER
══════════════════════════════════════ */
let currentPage = 'home';
function navigate(page) {
document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
document.getElementById(page).classList.add('active');
currentPage = page;
window.scrollTo(0,0);
if (page !== 'home') renderList(page);
updateCounts();
}
function updateCounts() {
const d = load();
const pl = n => n===1?'1 entry':n+' entries';
['verdict','audio','text'].forEach(t => {
const el = document.getElementById(t+'-count');
if (el) el.textContent = pl(d[t==='verdict'?'verdicts':t==='text'?'text':'audio'].length);
});
}
updateCounts();

/* ══════════════════════════════════════
RENDER LIST
══════════════════════════════════════ */
function renderList(type) {
const d = load();
const items = d[type] || [];
const q = (document.getElementById(type+'-search')?.value || '').toLowerCase();
const filtered = q ? items.filter(it => {
const haystack = [it.title,it.scholar,it.body,it.arabic,it.translation,
...Object.values(it.translations||{}).map(t=>t.text)].join(' ').toLowerCase();
return haystack.includes(q);
}) : items;

const el = document.getElementById(type+'-list');
if (!filtered.length) {
el.innerHTML = `<div class="empty-state"><div class="empty-ornament">☽</div><p>${q?'No results found.':'No entries yet. Add one above.'}</p></div>`;
return;
}
el.innerHTML = filtered.map(it => buildCard(it,type)).join('');
}

/* ══════════════════════════════════════
BUILD CARD
══════════════════════════════════════ */
function buildCard(it, type) {
const tagClass = {verdicts:'tag-verdict',audio:'tag-audio',text:'tag-text'}[type];
const tagLabel = {verdicts:'Fatwa',audio:'Audio',text:'Text'}[type];

let embed = '';
if (type==='audio' && it.url) {
const isYT = it.url.includes('youtube')||it.url.includes('youtu.be');
if (isYT) {
const yt = it.url.replace('watch?v=','embed/').replace('youtu.be/','www.youtube.com/embed/');
embed = `<div class="embed-area"><iframe width="100%" height="170" src="${e(yt)}" frameborder="0" allowfullscreen style="border-radius:3px"></iframe><span class="embed-url">${e(it.url)}</span></div>`;
} else {
embed = `<div class="embed-area"><audio controls src="${e(it.url)}"></audio><span class="embed-url">${e(it.url)}</span></div>`;
}
}

const scholar = it.scholar ? `<div class="scholar">— ${e(it.scholar)}</div>` : '';
const body = it.body ? `<div class="body-text">${e(it.body).replace(/\n/g,'<br>')}</div>` : '';
const arabic = it.arabic ? `<div class="arabic-text">${e(it.arabic)}</div>` : '';
const entrans = it.translation ? `<div class="en-translation">${e(it.translation)}</div>` : '';

// ── translations panel ──
const translations = it.translations || {};
const codes = Object.keys(translations);

let tabs = '';
let panes = '';

if (codes.length > 0) {
codes.forEach((code, i) => {
const tr = translations[code];
const lg = langOf(code);
const dot = tr.vetted ? `<span class="vetted-dot"></span>` : `<span class="draft-dot"></span>`;
tabs += `<div class="lang-tab${i===0?' active':''}" id="lt-${it.id}-${code}"
onclick="switchTab('${it.id}','${code}')" title="${lg.name}${tr.vetted?' · Vetted':' · Draft'}">
${dot}${e(lg.native)}
</div>`;

const badge = tr.vetted ? `<span class="vetted-badge">✓ Vetted</span>` : `<span class="draft-badge">Draft</span>`;
const cls = lg.rtl ? 'trans-text rtl-text' : 'trans-text';
const notes = tr.notes ? `<div class="trans-notes">📝 ${e(tr.notes)}</div>` : '';
const vBtn = tr.vetted
? `<button class="trans-action-btn btn-vett" onclick="setVett('${type}','${it.id}','${code}',false)">Unvet</button>`
: `<button class="trans-action-btn btn-vett" onclick="setVett('${type}','${it.id}','${code}',true)">Mark Vetted</button>`;

panes += `<div class="lang-content${i===0?' active':''}" id="lc-${it.id}-${code}">
<div class="trans-meta">
<span class="trans-lang-name">${e(lg.name)}</span>
${badge}
</div>
<div class="${cls}" style="font-family:${lg.font}">${e(tr.text)}</div>
${notes}
<div class="trans-actions">
${vBtn}
<button class="trans-action-btn btn-edit-t" onclick="openTransModal('${type}','${it.id}','${code}')">Edit</button>
<button class="trans-action-btn btn-del-t" onclick="delTrans('${type}','${it.id}','${code}')">Remove</button>
</div>
</div>`;
});
} else {
panes = `<div class="no-trans-pane">No translations yet — click <strong>+ Add Translation</strong> to add one in Swahili, Urdu, Russian, Malay, and more.</div>`;
}

const transPanel = `<div class="translations-panel">
<div class="trans-panel-header">
<span class="trans-panel-label">Translations</span>
<div class="lang-tabs">${tabs}</div>
<button class="add-trans-btn" onclick="openTransModal('${type}','${it.id}',null)">+ Add Translation</button>
</div>
${panes}
</div>`;

return `<div class="benefit-card" id="card-${it.id}">
<button class="delete-btn" onclick="delItem('${type}','${it.id}')">🗑</button>
<div class="card-main">
<span class="tag ${tagClass}">${tagLabel}</span>
<h4>${e(it.title||'Untitled')}</h4>
${scholar}${body}${arabic}${entrans}${embed}
<div class="card-date">${new Date(it.date).toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'})}</div>
</div>
${transPanel}
</div>`;
}

function switchTab(itemId, code) {
const card = document.getElementById('card-'+itemId);
if (!card) return;
card.querySelectorAll('.lang-tab').forEach(t => t.classList.remove('active'));
card.querySelectorAll('.lang-content').forEach(p => p.classList.remove('active'));
document.getElementById(`lt-${itemId}-${code}`)?.classList.add('active');
document.getElementById(`lc-${itemId}-${code}`)?.classList.add('active');
}

function e(s) {
return String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
}

/* ══ DELETE BENEFIT ══ */
function delItem(type, id) {
if (!confirm('Remove this entry?')) return;
const d = load(); d[type] = d[type].filter(x => x.id !== id);
save(d); renderList(type); updateCounts();
}

/* ══ VETT ══ */
function setVett(type, id, code, vetted) {
const d = load();
const it = d[type].find(x => x.id === id);
if (it?.translations?.[code]) { it.translations[code].vetted = vetted; save(d); renderList(type); }
}

/* ══ DELETE TRANSLATION ══ */
function delTrans(type, id, code) {
if (!confirm('Remove this translation?')) return;
const d = load();
const it = d[type].find(x => x.id === id);
if (it?.translations) { delete it.translations[code]; save(d); renderList(type); }
}

/* ══════════════════════════════════════
MAIN BENEFIT MODAL
══════════════════════════════════════ */
let modalType = '';
let transCtx = null; // { type, itemId, code } or null

function openModal(type) {
modalType = type; transCtx = null;
document.getElementById('modal-title').textContent =
{verdicts:'Add Verdict · فتوى', audio:'Add Audio Clip · مقطع صوتي', text:'Add Text Benefit · فائدة نصية'}[type];
document.getElementById('modal-body').innerHTML = buildBenefitForm(type);
document.getElementById('modal-overlay').classList.add('open');
}

function buildBenefitForm(type) {
const scholar = `<div class="field"><label>Scholar / Source</label><input type="text" id="f-scholar" placeholder="e.g. Shaykh Ibn Bāz رحمه الله"></div>`;
const body = `<div class="field"><label>Body / Summary (English)</label><textarea id="f-body" placeholder="Write the benefit or summary…"></textarea></div>`;

const arabic = `<div class="field"><label>Arabic Text <span class="rtl-toggle" onclick="toggleRTL('f-arabic')">toggle RTL</span></label><textarea id="f-arabic" dir="rtl" placeholder="النص بالعربية…"></textarea></div>`;
const entrans = `<div class="field"><label>English Translation of Arabic</label><textarea id="f-translation" placeholder="English translation…"></textarea></div>`;
const url = `<div class="field"><label>Audio / Video URL</label><input type="text" id="f-url" placeholder="https://… (mp3, mp4, YouTube)"></div>`;

let extra = type===‘verdicts’ ? scholar+body+arabic+entrans
: type===‘audio’ ? scholar+url+entrans+body
: scholar+arabic+entrans+body;

return `<div class="field"><label>Title (English)</label><input type="text" id="f-title" placeholder="e.g. Ruling on combining prayers while travelling"></div> ${extra} <button class="save-btn" onclick="saveItem()">Save Benefit</button>`;
}

function toggleRTL(id) { const el=document.getElementById(id); if(el) el.dir = el.dir===‘rtl’?‘ltr’:‘rtl’; }
function v(id) { const el=document.getElementById(id); return el?el.value.trim():’’; }

function saveItem() {
const title = v(‘f-title’);
if (!title) { alert(‘Please enter a title.’); return; }
const it = {
id: Date.now().toString(36)+Math.random().toString(36).slice(2),
date: new Date().toISOString(),
title, scholar:v(‘f-scholar’), body:v(‘f-body’),
arabic:v(‘f-arabic’), translation:v(‘f-translation’), url:v(‘f-url’),
translations: {},
};
const d = load(); d[modalType].unshift(it);
save(d); closeModal(); renderList(modalType); updateCounts();
}

/* ══════════════════════════════════════
TRANSLATION MODAL
══════════════════════════════════════ */
function openTransModal(type, itemId, code) {
transCtx = { type, itemId, code };
const d = load();
const it = d[type].find(x => x.id === itemId);
const existing = code ? it?.translations?.[code] : null;

document.getElementById(‘modal-title’).textContent =
code ? `Edit · ${langOf(code).name}` : ‘Add Translation’;

let langSel = ‘’;
if (!code) {
const added = Object.keys(it?.translations || {});
const avail = LANGUAGES.filter(l => !added.includes(l.code));
if (!avail.length) {
document.getElementById(‘modal-body’).innerHTML =
`<p style="color:var(--green-mid);font-style:italic;margin-top:.5rem">All supported languages have already been added to this entry.</p>`;
document.getElementById(‘modal-overlay’).classList.add(‘open’);
return;
}
langSel = `<div class="field"><label>Language</label> <select id="f-lang"> <option value="">— choose language —</option> ${avail.map(l=>`<option value="${l.code}">${l.name} · ${l.native}</option>`).join('')} </select></div>`;
} else {
const lg = langOf(code);
langSel = `<div class="field"><label>Language</label> <input type="text" disabled value="${e(lg.name+' · '+lg.native)}" style="opacity:.6;cursor:not-allowed"></div>`;
}

const prefText = existing?.text || ‘’;
const prefNotes = existing?.notes || ‘’;
const prefVet = existing?.vetted || false;

document.getElementById(‘modal-body’).innerHTML = ` ${langSel} <div class="field"> <label>Translation Text <span class="rtl-toggle" onclick="toggleRTL('f-trans-text')">toggle RTL</span></label> <textarea id="f-trans-text" style="min-height:110px" placeholder="Enter the translation in this language…">${e(prefText)}</textarea> </div> <div class="field"> <label>Translator Notes (optional)</label> <textarea id="f-trans-notes" placeholder="Context, vetter name, regional notes…">${e(prefNotes)}</textarea> </div> <div class="vet-row"> <input type="checkbox" id="f-vetted" ${prefVet?'checked':''}> <label for="f-vetted">Vetted — approved by a language volunteer</label> </div> <button class="save-btn" onclick="saveTrans()">Save Translation</button>`;

document.getElementById(‘modal-overlay’).classList.add(‘open’);
}

function saveTrans() {
if (!transCtx) return;
const code = transCtx.code || v(‘f-lang’);
if (!code) { alert(‘Please choose a language.’); return; }
const text = v(‘f-trans-text’);
if (!text) { alert(‘Please enter the translation.’); return; }
const notes = v(‘f-trans-notes’);
const vetted = document.getElementById(‘f-vetted’)?.checked || false;

const d = load();
const it = d[transCtx.type].find(x => x.id === transCtx.itemId);
if (!it) return;
if (!it.translations) it.translations = {};
it.translations[code] = { text, notes, vetted, updatedAt: new Date().toISOString() };
save(d);
closeModal();
renderList(transCtx.type);
}

function closeModal() { document.getElementById(‘modal-overlay’).classList.remove(‘open’); transCtx=null; }
function closeModalOutside(ev) { if (ev.target===document.getElementById(‘modal-overlay’)) closeModal(); }
</script>

</body>
</html>
